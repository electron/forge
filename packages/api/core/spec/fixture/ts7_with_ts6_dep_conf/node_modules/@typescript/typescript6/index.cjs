// Stand-in for Microsoft's real `@typescript/typescript6` package, which
// re-exports the classic TypeScript compiler API for tools that need it
// alongside a TypeScript 7 install. Requiring `typescript` from here would
// hit this fixture's v7-shaped stub, so resolve the monorepo's real
// TypeScript from @electron-forge/core's own dependencies instead.
const { createRequire } = require('node:module');
const path = require('node:path');

// __dirname -> <fixture>/node_modules/@typescript/typescript6; six levels up
// is packages/api/core.
const coreDir = path.resolve(__dirname, '..', '..', '..', '..', '..', '..');
module.exports = createRequire(path.join(coreDir, 'noop.js'))('typescript');
