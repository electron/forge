// import { expect } from 'chai';

// const mockProjectDir = process.platform === 'win32' ? 'C:\\path' : '/path';

describe('ElectronForgeLogging', () => {
  describe('Triggers on stats', () => {
    // TODO: Find how to get a trigger from tab
  });
});
